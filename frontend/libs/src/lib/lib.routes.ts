import { Route } from '@angular/router';
import { LibPlotkyComponent } from './share/ploty/components/lib.ploty.component';

export const sharedRoutes: Route[] = [
    {
        path: '',
        component: LibPlotkyComponent
    }
];
